=======================================================================
= Building mutt.1.5.20
=======================================================================
Build Machine:
	Thor
	Linux thor 2.6.38-8-generic #42-Ubuntu SMP Mon Apr 11 03:31:24 UTC 2011 x86_64 x86_64 x86_64 GNU/Linux
	Linux Mint 11 Katya \n \l

Build Commands:
	. /usr/share/cross-compile/arm-sourcery-glibc/SOURCE_ME
	./configure --host arm-linux-gnu CC=${CROSS_COMPILE}gcc --with-curses=../ncurses-5.9

	// The compile step will fail for documentation, therefore do the following:
	//	- Remove "doc" from the line "DIST_SUBDIRS = m4 po intl doc contrib imap" in the created ./Makefile
	//	- Remove "doc" from the line "SUBDIRS = m4 po intl contrib $(IMAP_SUBDIR)" in the created ./Makefile

	make -j 4
	
